# --- Database -----------------------------------------------------------
DATABASE_URL=postgres://frandee:frandee_pass@localhost:5432/frandee

# --- Auth ---------------------------------------------------------------
# Generate with: openssl rand -hex 32
JWT_SECRET=replace_with_64_char_random_hex_string

# --- Server -------------------------------------------------------------
PORT=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
FORCE_HTTPS=false

# --- Mail (contact form) -----------------------------------------------
# Leave blank in dev to log instead of send.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO=info@frandeeconsult.com

# --- Sentry (optional) -------------------------------------------------
SENTRY_DSN=
